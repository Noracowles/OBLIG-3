def my_str_len(data):
    lengths= [] #Lager variabelen legths og putter en tom liste i den
    for word in data: #For alle "ord" i listen data så:
        lengths.append(len(word)) #len: lengden på word, append legger den bakerst i listen
    return lengths #return sender lengths tilbake til der vi kalte funksjonen

def my_max(numbers):
    biggest=numbers[0]
    for num in numbers:
        if num > biggest:
            biggest = num
    return biggest

data= ("hei", "genser", "skole", "", "smil", "janis") #Lager en variabel med navn data.
answer= my_str_len(data) #lager en variabel med navn answer. Putter inn det som skal returneres
print (answer) #sender ned verdien jeg har puttet i "answer"

numbers= (3, 6, 10, 2,)

res= my_max(numbers)
